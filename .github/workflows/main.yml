name: Deploy via ftp
on: push
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Upload ftp
        uses: sebastianpopp/ftp-action@releases/v2
        with:
          host: ftp.server589298.nazwa.pl
          user: "server589298_www@ftp.server589298.nazwa.pl"
          password: "Janekz0awww"
          options: "--delete --asci"